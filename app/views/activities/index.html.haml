%div{class: "page-header"}
  %h3
    = t('activities')
  
- @activities.each do |activity|

  %div

    %header
      %h4{clas: "page-header"}=  link_to activity.title, activity_path(activity.id) 
    %p{class: "content"}= truncate_html activity.description.html_safe, length: 200
    %aside
      %small{class: "pull-right"}= activity.created_at.strftime('%d.%m.%Y')
      - if current_admin_user
        |
        = link_to 'Show', activity
        |
        = link_to 'Edit', edit_activity_path(activity)
        |
        = link_to 'Destroy', activity, :method => :delete, :data => { :confirm => 'Are you sure?' }
  %div{class: "clearfix"}
  %hr

%br
= link_to 'New activity', new_activity_path, class: "btn btn-primary" if current_admin_user
